name: Test workflow
on: [push]
jobs:
   build:
     runs-on: ubuntu-latest
     steps:
         - name: step1
           run: echo "This is a test"
         - name: step2
           uses: actions/checkout@4.1.4
         - name: step3
           run: mvn compile
       
  
